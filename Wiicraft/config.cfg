API=true 		    				;if you want to use API(recomended to be true only have this to false if you debuging.)
TEXTURES=/textures          				;the path there you have your texture files
ADDBLOCKS=false              				;if true so will it use world.map WIP
ADDBLOCKSPATH=APPS/Minecraft_Wii_Edition/world.map   	;the path there you have your world.map file. if you use sd or usb is auto detected.